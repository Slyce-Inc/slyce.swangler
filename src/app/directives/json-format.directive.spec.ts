import { JsonFormatDirective } from './json-format.directive';

describe('JsonFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonFormatDirective();
    expect(directive).toBeTruthy();
  });
});
